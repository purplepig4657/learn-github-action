name: Build and Deploy React App
on:
    push:
        branches:
        - main
jobs:
    test:
        runs-on: ubuntu-latest
        steps: 
            - run: echo "Hello, world!"
            - name: Checkout Code
              uses: actions/checkout@v3
            
            - name: File list
              run: ls -alt
            
