name: test
on: [push]
jobs:
    test:
        runs-on: ubuntu-lastest
        steps: 
            - run: echo "Hello, world!"
            - uses: actions/checkout@v3
              with:
                repository: ''
